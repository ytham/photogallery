<% provide(:title, @user.name) %>
<h1>
  <%= @user.name %>
</h1>

Submitted Photos:
<% if @photo_list != nil %>
  <ul>
    <% @photo_list.each do |p| %>
      <li><%= link_to remove_underscores(p.name), p %></li>
    <% end %>
  </ul>
<% else %>
  No photos.
<% end %>

<% if current_user == @user %>
  <p><%= link_to 'Log Out', logout_path %></p>
<% end %>
